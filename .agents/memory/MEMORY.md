- [Adding tables — _DDL vs Alembic](schema-ddl-vs-alembic.md) — new DB tables need an Alembic migration; the legacy schema.py `_DDL` silently fails partway on existing DBs so appended tables never get created.
- [cost_dashboard feature gate vs tests](cost-dashboard-feature-gate.md) — cost HTTP tests 404 when the feature flag is toggled off in tenant_features; it's a data/toggle state, not a merge break.
- [Public-site gating must exempt machine endpoints](chat-only-allowlist.md) — a before_request allowlist that hides public pages must also let /webhooks /automations /plugin through or it silently swallows webhook deliveries.
- [Streaming page-build infra](streaming-page-build-infra.md) — SSE/AI-stream endpoints need gthread + long gunicorn --timeout (else WORKER TIMEOUT kills them) + big per-round max_tokens + ack-aware iframe teardown; `winch` log flood is harmless.
- [pytest suite env sensitivity](test-suite-env-sensitivity.md) — `pytest tests/` in the live workspace shows many FALSE failures (leaking SUPER_ADMIN_KEY/secrets, missing SSO_SIGNING_SECRET/CLIENT_PASSWORD); run with a clean env; trust app-boot+migrations as the real health signal.
- [Dual boot entrypoints](boot-entrypoints.md) — boot-time seeding/sync must be added to BOTH main._bootstrap() and app.py __main__; write-invalidated caches must take the loader's lock.
- [query_db fetchone gotcha](query-db-fetchone.md) — query_db returns a LIST by default; single-row callers using row.get(...) need fetchone=True or it raises and is often silently swallowed.
- [immersive "Building" overlay teardown](immersive-page-stream-teardown.md) — visitor page-builder freezes on "Building" when a stream exit path skips the command handler; tear the overlay down on error AND ready-timeout, not just success.
- [generated_pages reuse policy](generated-pages-reuse-policy.md) — the "saved AI page" reuse status filter lives in ~6 spots (PAGE LIBRARY prompt, lookup tool x2, by-slug x2, a draft-list prompt block); change them in lockstep or the model gets contradictory signals.
- [Embed widget — live copy & shadow overlay](embed-widget-copies.md) — only `embed/widget/*` is served live (`admin_ai_platform/web/*` is a mirror); overlay must mount in the Shadow DOM, theme via `/api/theme`.
- [OpenAI client key wiring](openai-client-key-wiring.md) — chat/embeddings use the Replit proxy var AI_INTEGRATIONS_OPENAI_API_KEY; voice uses direct OPENAI_API_KEY. Adding only OPENAI_API_KEY won't fix chat unless a direct-key fallback exists.
- [Visitor chat tool-round invariant](visitor-chat-tool-rounds.md) — the visitor /api/chat loop's last round MUST be tool-less or many-tool turns return empty & strand the UI on "thinking"; round budget is admin-controlled (normal/complex knobs).
- [Public JS served as a cached bundle](public-js-bundle-cache.md) — public/script.js + voice.js are concatenated into a content-hashed bundle built once per worker; editing script.js requires a workflow RESTART (gunicorn --reload only watches .py) or the browser keeps the old code.
